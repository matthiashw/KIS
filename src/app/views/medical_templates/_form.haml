- semantic_form_for @medical_template do |f|
    = f.error_messages
    - f.inputs do
        = f.input :name
        = f.input :domain_id, :as => :select , :collection => Domain.userdomains
    = f.buttons