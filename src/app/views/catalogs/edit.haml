- title t('admin.catalog.edit')

- semantic_form_for @catalog do |f|
  = f.error_messages
  - f.inputs do
    = f.input :year
    = f.input :language
  = f.buttons

.action-links
  = link_to t('action.back'), catalogs_path, :class => 'acts_as_button darkgray'
