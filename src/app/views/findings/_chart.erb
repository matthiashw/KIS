<script type="text/javascript">
<!--

var units = [<%  @units.each do |unit| %>'<%=  unit %>', <%  end %>]

function updateChart(){
  var field = Ext.get('fields-history')
  var fieldId = field.getValue(true);
  var name = field.dom[field.dom.selectedIndex].innerHTML;
  
  var store = getChartStore('findings/chartdata/' + fieldId + '.json');
  store.load();
  var panel = getChartPanel(store, name, units[field.dom.selectedIndex]);

  var window = new Ext.Window({
    title:  'Findings - Statistic',
    items: [panel]
  });

  window.show();
}

//-->
</script>
