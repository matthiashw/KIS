- title t('admin.catalog_type.new')

= render :partial => 'form',:locals => { :active_catalogs_select => false}

.action-links
    = link_to t('action.back'), catalog_types_path, :class => 'acts_as_button darkgray'

