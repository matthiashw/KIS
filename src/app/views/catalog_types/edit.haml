%h1 Editing catalog_type
- form_for(@catalog_type) do |f|
  = f.error_messages
  %p
    = f.label :name
    %br/
    = f.text_field :name
  %p
    = f.submit 'Update'
= link_to 'Show', @catalog_type
|
\#{link_to 'Back', catalog_types_path}
