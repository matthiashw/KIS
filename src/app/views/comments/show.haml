%p
    %b=t('comment.string') + ":"
    = h @comment.comment
%p
    %b=t('comment.date') + ":"
    = h @comment.date

.action-links
    = link_to t('action.edit'), edit_comment_path(@comment), :class => 'acts_as_button'
    = link_to t('action.back'), comments_path, :class => 'acts_as_button darkgray'
