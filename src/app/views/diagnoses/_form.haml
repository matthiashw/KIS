- semantic_form_for @diagnosis do |f|
    = f.error_messages

    - f.inputs do
        = f.input :case_file_id
        = f.input :description, :input_html => { :rows => 4, :cols => 30 }
    = f.buttons
