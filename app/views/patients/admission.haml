-title t('patient.admission')

.action-links.text-center
    - if authorize?('create_patient')
        = link_to t('patient.new_button'), new_patient_path, :class => "acts_as_button"

= render :partial => 'patient_search_complete'

